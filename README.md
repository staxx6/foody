# Foody

## Requirements

- pnpm

## Setup

Run `pnpm i` on root folder to install dependencies.
Download PocketBase from here: https://pocketbase.io/docs/ and place files under `apps/pocketbase`.

## Start everything

`pnpm run serve` to start everything

